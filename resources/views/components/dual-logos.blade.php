<div class="w-full h-auto flex justify-between items-center p-2 {{ $containerClass }}">
    <!-- Logo izquierdo -->
    <img src="{{ asset($leftLogo) }}" class="{{ $leftSize }} object-contain" alt="Logo izquierdo">

    <!-- Logo derecho -->
    <img src="{{ asset($rightLogo) }}" class="{{ $rightSize }} object-contain" alt="Logo derecho">
</div>
